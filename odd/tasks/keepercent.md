# Keepercent — task tracking

Feature document: [docs/mvp.md](../../docs/mvp.md) · Deadline: **Friday Sep 25, 2026**
TDD: strict mode enabled for the Domain layer (RED → GREEN → REFACTOR), Swift Testing.

## Day 0 — Sat Sep 19 (setup)

- [x] T0.1 MVP definition written (`docs/mvp.md`) — inline
- [x] T0.2 Repository scaffolding: `.gitignore`, `README.md`, `CLAUDE.md`, task file — inline
- [x] T0.3 Install Xcode — Xcode 27.0 (27A266a) installed; organizers confirmed iOS 26 as the minimum, so no Xcode 26 needed
- [x] T0.4 Create the Xcode project `Keepercent` — hand-written pbxproj (synchronized group, shared scheme, local Domain package); `xcodebuild ... build`: BUILD SUCCEEDED, `-target ...ios26.0-simulator -swift-version 6`
- [x] T0.5 First commit + GitHub repository — https://github.com/tomioliveres/keepercent (public)
- [x] T0.6 Daily update posted in `#daily-updates`
- [x] T0.7 Documentation pass: architecture explained from first principles in `docs/index.html` §06 (dependency rule, why `Domain/` is a separate package, screaming architecture, ports/adapters, container/presentational split, why no repository over SwiftData, the `StatsEngine`/`ModelContext` invariant), new `docs/index.html` §07 on build methodology (TDD, test scope, RDD vs testing, work-unit commits, sequential branch), decisions table extended, `docs/mvp.md` §8 and `CLAUDE.md` updated to match — direct inline (documentation-only, no source code touched, single author reviewing every line); verified: section numbering sequential (`rg -n '<span class="num">'`), HTML parses (`python3 -c "html.parser..."`), section/h3 tag counts balanced

## Day 1 — Sun Sep 20 (domain)

- [x] T1.1 `GoalTarget`: 3×3 inside grid, post segments, out directions + tests — delegated writer (2+ files); `swift test`: 11 tests / 55 cases passed
- [x] T1.2 `CourtZone` + `CourtGeometry`: sector from the angle at the goal centre (cuts at ±18°/±54°), depth from the distance to the **goal mouth** so it matches the drawn 9m line, `ShotOrigin` with the 7m case, persistence codes — delegated writer (4 files, TDD RED→GREEN→REFACTOR); `swift test`: 41 tests / 16 suites passed (parent spot check re-ran it); `gentle-ai review assess`: medium, RDD off → writer self-verification + spot check
- [x] T1.3 `ShotClassification`: `ShotSide`/`ShotHeight`/`ShotLine` derived from an origin/target pair; classification covers all three `GoalTarget` cases (post segments and out directions carry a side, crossbar -> `top`), height only where the vertical band inside the frame is known (every `out` case, `over` included, has none) — delegated writer, strict TDD with the RED run shown before GREEN (2 files); `swift test --package-path Domain`: 54 tests / 21 suites passed (parent spot check re-ran it); commit `59aa16f`; `gentle-ai review assess`: medium (`executable_change`, 309 lines) -> consent granted, lens `review-reliability` returned **approved** (lineage `review-5355880acf646521`, acknowledged, authority burned) with two non-blocking advisory findings, both verified against the code and applied as the follow-up commit: R3-001 replaced the `default` clause in `line(from:to:)` with explicit `(.left, .right), (.right, .left)` patterns so exhaustiveness is compiler-verified instead of test-verified, and R3-002 added the missing end-to-end assertion for `post(.crossbarCenter)` -> `.neutral`
- [x] T1.4 SwiftData models + mapping to domain values (primitive codes) — split into two work units because the Xcode project has a single app target and **no test target**: everything testable stays in `Domain/`, the `@Model` types are a thin shell over primitives (`docs/mvp.md` §8). `a5fbc55` domain value types (`Player`, `Handedness`, `SessionKind`, `AttackingSide`, `ShotDelivery`, `ShotApproach`, `Shot` with derived `origin`/`line`) — delegated writer, strict TDD, RED shown before GREEN; parent removed a redundant `NormalizedPoint` alias over the existing `CourtPoint` and a duplicate clamping suite, and renamed `Shot.classification` -> `Shot.line`. `eef4cb9` the initializer now drops an `originPoint` passed with `isSevenMeters`, making the contradictory pair unrepresentable instead of merely ignored. `3a5bee4` `Keepercent/Persistence/`: `StoredRivalTeam`, `StoredPlayer`, `StoredSession`, `StoredShot`, the schema and the container wiring — delegated writer; parent added the missing `@Relationship` inverses (`StoredPlayer.shotsTaken`/`.shotsFaced`, `StoredRivalTeam.sessions`) after verifying against Apple's SwiftData documentation that a delete rule reaches referencing rows only through an inverse, so the writer's claimed nullify behaviour was unsupported. `ef3056b` review correction: the decoding of stored codes moved into the domain as `Shot.init?(attackingSideCode:...)` and `Player.init(...handednessCode:)`, next to `GoalTarget.init?(code:)`. `36bb1b7` coverage held back from that bounded correction. Verified: `swift test --package-path Domain`: 79 tests / 30 suites passed (parent spot check re-ran it); `xcodebuild -project Keepercent.xcodeproj -scheme Keepercent -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.1' build`: `** BUILD SUCCEEDED **`; app installed and launched on the simulator with the SwiftData container open (a build alone never proves a schema loads). RDD: `a5fbc55` assessed medium -> consent granted, lens `review-reliability` **approved** (lineage `review-018a519c56a99fde`, acknowledged) with two advisory findings, both verified and applied as `eef4cb9`. The `3a5bee4` slice assessed medium -> consent granted, lens `review-reliability` returned **`correction_required`** with two CRITICAL findings (R3-001, R3-002: the persistence mapping had no test at all); one bounded correction of exactly 200 lines answered both, targeted validation passed, **approved** (lineage `review-b6f5974c6d76aa26`, acknowledged, authority burned). `36bb1b7` assessed medium, 20 lines, deferred to the next slice.
- [x] T1.5 Demo data seed — one rival team, a twelve-player roster (2 goalkeepers, 10 field players) and a full live session, built entirely from fixed inputs so the app opens on something that reads as real scouting. `7d23186` `Domain/Sources/KeepercentDomain/DemoData.swift` (388 lines) plus `DemoDataTests.swift` (246 lines): the dataset lives in the domain, where it is testable without a simulator, and the tests assert the *tendencies* rather than the rows — both attacking sides are represented, every shot respects the attacking-side invariant, the demo shooter's bias is a strict majority, origins resolve through `CourtGeometry`, and two evaluations are identical. No `Date()` and no randomness anywhere: `sessionDate` is built from explicit UTC date components, so the whole dataset stays on one calendar day whenever it is read. `40541e6` `Keepercent/Persistence/DemoDataSeeder.swift` (54 lines): a fetch limited to one row short-circuits when the demo team already exists, so the seed is idempotent across launches; the graph is assembled before `context.insert(team)`, because SwiftData follows the relationships from the single inserted root. Players are keyed by shirt number so a shooter and the goalkeeper facing them map to the same stored row instead of inserting a player twice. Verified: `swift test --package-path Domain`: 106 tests / 38 suites passed (parent spot check re-ran it); `xcodebuild -project Keepercent.xcodeproj -scheme Keepercent -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.1' build`: `** BUILD SUCCEEDED **`. RDD: reviewed as one accumulated slice from the last reviewed boundary `ef3056b` with `--base-ref ef3056b --committed-only`, covering `36bb1b7`, `00dc358`, `df315bd`, `7d23186` and `40541e6` — the selectorless status would have started from `ce090d3` and re-dragged the whole already-acknowledged T1.4 block. Assessed medium (`executable_change` in `DemoData.swift`, 5 paths, 723 lines) -> consent granted, lens `review-reliability` returned **approved** (lineage `review-ed63edf490bcc819`, acknowledged, authority burned) with two non-blocking advisory findings and no blockers. **Both advisory findings were verified against the code and deliberately not applied**, because each proposed fix trades a loud, correct failure for a quiet, wrong one. R3-001 (`DemoDataSeeder.swift`, `Dictionary(uniqueKeysWithValues:)` traps on a duplicate shirt number): the roster is a hand-written static fixture, so a duplicate is a programmer error and the trap makes it visible at first launch; `uniquingKeysWith:` would silently drop a player and misattribute every statistic derived from her. The invariant is stated in a comment at the call site and asserted in `DemoDataTests`. Revisit the day the roster stops being a static fixture and arrives from outside the binary — then it needs validation at the boundary. R3-002 (`DemoData.swift`, `sessionDate` force-unwraps `Calendar.date(from:)`): every component is an integer literal in a Gregorian calendar, so the nil branch cannot be taken; a `guard let` would require inventing a meaningless fallback date for an unreachable path. The clarifying comment rides with the next task's commits rather than a standalone commit, which would open a fresh medium candidate for one line. Afterwards the stop hook re-offered the selectorless candidate from `ce090d3` — 12 paths, 1498 lines, the whole already-acknowledged T1.4 and T1.5 block, plus two lines of markdown — and it was **declined** (`declined_this_candidate`, lineage `review-6108de81abe613ee`); candidate-scoped, reviews stay enabled.

## Day 2 — Mon Sep 21 (components)

- [x] T2.1 `GoalView`: drawing + hit-testing. The task originally read "frame areas enlarged beyond the stroke"; that was tried and **reversed after testing it by hand** — see the "what you see is what you tap" note below. Split into two work units, because the goal maths belongs in `Domain/` for the same reason as T1.2: the app target has no test target. `3ddc274` `GoalGeometry` (362 lines) + tests (279 lines) — delegated writer, strict TDD, RED shown before GREEN. `GoalPoint` deliberately does **not** clamp to `0...1`, unlike `CourtPoint`: on the court an out-of-range tap always means "somewhere on the court" and clamping loses nothing, but on the goal out-of-range *is* the information — it is what a post hit and a miss look like, and clamping would turn both into zones inside the frame. Tested tie-breaks, each deliberate: the corner belongs to the **post** (the crossbar spans *between* the posts, so its band's `x` is restricted to the mouth), a tap both high and wide reads as wide (`MissDirection` has no combined case), a tap below the ground line resolves onto it (the ball cannot pass under the floor), NaN resolves to the mouth centre. The writer flagged an open question that turned out to be a real defect: the post band had no upper bound on `y` while `region(for:)` stops at `-bandY`, so everything above the drawn region still hit-tested as the post — drawing and hit-testing had drifted, the exact thing `region(for:)` exists to prevent. **The round-trip test missed it because it only checked region centres**; it now asserts all four edge-inset points too. `d43d70d` `Keepercent/DesignSystem/GoalView.swift` — delegated writer; the parent then fixed two defects that a green build and 142 green tests could not see, both found by running it: the aspect ratio was computed in normalized units, **which are not square** (one `x` unit spans 3 m, one `y` unit 2 m), drawing a nearly square goal clipped at the bottom — fixed by converting both back to metres and using two independent axis scales from a single shared `layout(in:)` that drawing and hit-testing both read; and the 3×3 grid was drawn in the same colour and weight as the net, so the nine zones — the actual affordance — were invisible. `5ed48af` **user-found defect**: tapping the painted black post recorded `inside.middle.left`, because the stroke was centred on the mouth's edge and half its width fell inside. The frame is now *filled* as exactly the region `GoalGeometry` resolves to `.post(...)` — **what you see is what you tap**: one out band, one frame, one mouth, where before there were four bands and no way to tell where the post ended. The drawn post is deliberately not to scale (a real post is ~8 cm against a 3 m mouth). `28c78ab` the review's two advisory findings, both verified and applied: `GoalGeometry.init` now traps a degenerate geometry, because a zero width made the band thickness infinite and every tap would have silently resolved as a post hit — a quiet wrong answer that corrupts recorded data, where a trap is loud and correct; and the dead upper bound in `insideMouthY` (already guaranteed by `groundResolvedY`) was removed rather than left looking protective, plus four tests resolving targets against a non-default 4×2 geometry, since every earlier test used only `.standard`. Verified: `swift test --package-path Domain`: 142 tests / 47 suites passed (parent spot check re-ran it); `xcodebuild -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.1'`: `** BUILD SUCCEEDED **`; launched on the simulator and tapped by hand — `inside.top.left`, `post.leftPostMiddle` (tapped *outside* the old drawn stroke, inside the hit band) and `out.wideLeft`; dark mode checked. **Not verified: iPad in landscape** — the machine's disk hit 100% and the simulator launch failed with `ENOSPC`. The component keeps its proportions in any container through `.aspectRatio(.fit)`; the landscape *screen layout* (court and goal side by side instead of stacked, which `docs/mvp.md` §5.2 still describes as a vertical path) is a T3.3 decision, recorded there. RDD: assessed with `--base-ref 7a05ed9 --committed-only`, this task's own slice — the last reviewed boundary `40541e6` would have dragged in the previous day's docs commits. Medium (`executable_change` in `GoalGeometry.swift`, 4 paths, 1094 lines) -> consent granted, lens `review-reliability` returned **approved** (lineage `review-0d4bf98c6cc9d51d`, acknowledged, authority burned) with no blockers. `28c78ab` is the follow-up applying its advisory findings and defers to the next slice.
- [x] T2.2 `CourtView`: drawing + hit-testing + 7m mark. Split into two work units for the same reason as T1.2 and T2.1 — the app target has no test target, so everything testable lives in `Domain/`. `15c47c8` the court's drawing surface (498 lines): `aspectRatio`, `line(atDistanceInMeters:)`, `sectorBoundaryRays`, `goalMouth`, `sevenMeterMarkRegion` and `origin(at:)`, plus `CourtRegion`/`CourtSegment` mirroring `GoalRegion` — delegated writer, strict TDD, RED shown before GREEN. The 6 m and 9 m lines are generated as a **polyline walking the same `distanceToGoalMouth` definition `depth(at:)` classifies against** — two quarter arcs on the posts joined by a straight segment, never a semicircle, sampled about a degree per step — so the drawn 9 m line **is** the hit-tested boundary by construction, and the property test asserts exactly that (every sampled point sits at distance *d* from the goal mouth). A part of the shape that leaves the drawn court is dropped rather than clamped: `CourtPoint` clamps to `0...1`, so clamping would have turned "the line left the court" into "the line runs back along the touchline". The ±18°/±54° literals moved out of `sector(at:)` into named constants both the rays and the classification read. The 7 m mark is **1.4 m wide by 1.0 m deep, wider than it is deep on purpose**: left and right of the mark you are still in the `center` sector so width is free, while depth is where the pivot shoots from — a disc big enough to be a comfortable 44 pt target would have swallowed pivot shots and recorded them as penalties. The parent added the degenerate-geometry trap the writer left out, because T2.1's review had already forced the same trap on `GoalGeometry` and a zero width collapses `xMeters` to 0, reading every tap as `center`. `ced9af7` `Keepercent/DesignSystem/CourtView.swift` — delegated writer. **One axis scale, not two**: unlike the goal, a normalized `CourtPoint` already spans the whole court on each axis, and `.aspectRatio(geometry.aspectRatio, .fit)` makes pixels-per-metre equal on both axes by construction, so a single `pixelPoint(for:in:)` serves drawing and hit-testing. The zone grid (9 m line plus the four rays) is dashed and heavier than the 6 m line **from the start** rather than after someone noticed — T2.1's invisible 3×3 grid, carried forward. Two defects a green build and 164 green tests could not see, both found by running it: the goal mouth sits at `y = 0`, so half its 6 pt stroke was clipped off the canvas and the goal read as floating above the court, and the outline lost half of every edge the same way — both inset **in the drawing only**, never in `pixelPoint(for:in:)`, which is the exact inverse the hit-testing depends on and would have moved every tap along with the ink; and the 6 m line at opacity 0.3 was illegible, which is backwards for the line a player reads the whole court against. Verified: `swift test --package-path Domain`: 166 tests / 52 suites passed (parent spot check re-ran it); `xcodebuild -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.1'`: `** BUILD SUCCEEDED **`; launched and tapped by hand — the mark's centre gave `sevenMeters`, 17 pt above it (just past the mark's top edge) gave `zone.center.near`, top-left gave `zone.leftWing.near` and bottom-right gave `zone.rightBack.far`, which is the check that catches an x-flip in the view since the domain is symmetric and no test can see a mirrored drawing; dark mode checked. **Not verified: iPad in landscape**, same as T2.1. The mark measures about 44×34 pt on an iPhone 17 Pro, so its height is just under Apple's minimum tap target — deliberate, per the pivot reasoning above, and flagged for the T6.1 accessibility pass. RDD: reviewed as this task's own slice with `--base-ref 5ed48af --committed-only` (the selectorless status would have started from `7a05ed9` and re-dragged T2.1's already-acknowledged `GoalView.swift`). The first `start` failed `managed_assets_outdated`; `gentle-ai sync` fixed it and the same target was re-offered. Medium (`executable_change` in `CourtGeometry.swift`, 7 paths, 824 lines) -> consent granted, lens `review-reliability` returned **approved** (lineage `review-ce01f6c2948cb4db`, acknowledged, authority burned) with no blockers and two advisory findings, both verified against the code before acting. R3-002 was exact and applied. **R3-001's stated failure mode does not hold**: it claimed that removing `groundResolvedY` would let a `y` past the floor resolve silently as a zone, but the post bands re-clamp on their own and `rowFor(1.5)` buckets into `.bottom` exactly as `rowFor(1.0)` does, so the observable answer is identical with or without that clamp. The gap underneath it was real, though — nothing tested that a tap under the floor lands on the floor at all — so `c88de08` pins that behaviour, inside the mouth and in a post band, rather than the step that currently upholds it. `c88de08` defers to the next slice.
- [x] T2.3 Selection highlighting — a tap now tints the region it resolved to, on both views. Four commits: `87914a7` `GoalTarget` drawable regions (misses included), `e367e52` a fillable polygon for every `CourtZone` that agrees with `zone(at:)`, `c5f5657` the miss-area fix below, `718d068` the views reading `selection`. **The 38 red tests had one root cause**: `CourtGeometry.shape(for:)` sampled its boundary at uniform 1° steps, but both boundary functions are **piecewise** — every elbow fell between two samples and the chord cut the corner off. Sampling finer does not fix it: an elbow's error is O(1), not O(step²). Three elbows now enter as exact vertices — the court corner (the gap it used to leave measured ~15 cm), the angle where the 9 m line exits the court (found by bisection; there is no closed form), and the straight→arc junction at the post. Inside each piece the samples were already collinear, so the elbow was the only structural error. What is left is irreducible chord-over-arc sagitta, **measured at 0.36 mm**: the test that demanded exact equality between the chorded polygon and the analytic classifier was asserting something false. It now forgives 5 mm on the 9 m curve and **nothing else**, bought back with exact assertions (a vertex within 1e-9 of `nineMeterLine`, the court corner as a literal vertex). Parent correction over the writer: the tolerances for sector cuts and court edges were written and then **deleted** — removed one at a time, the suite stayed green without them, and the sector one re-typed `18.0`/`54.0`, the exact drift `centerBoundaryDegrees` exists to prevent, in the one file whose job is to catch that drift. **The defect 182 green tests could not see**: `region(for: MissDirection)` (`87914a7`, the parent's own commit) returned a finite rect documented as a guaranteed *subset* of the miss area — a true contract in the wrong shape. `GoalView` tinted those rects, so the margin outside them stayed grey while still hit-testing as a miss; tapping (40, 245) on the simulator, plainly outside the ink, recorded `out.wideLeft`. T2.1's "what you see is what you tap" all over again, which is why `c5f5657` replaces it with `regions(for:within:)`, tiling exactly into five rects taken from `target(at:)`'s own fallback. Colour correction: the writer gave the misses three different greys; `wideLeft` and `wideRight` now share a tint, because they are the same miss mirrored and never touch — position already tells them apart, so the colour carries wide-versus-high instead. Verified: `swift test --package-path Domain`: 182 tests / 55 suites passed; launched on the simulator and tapped by hand. **Deferred to T6.1**: the blue selection tint over the black post is low-contrast. RDD: reviewed as this task's own slice with `--base-ref ced9af7 --committed-only` (9 paths, 1100 lines) -> consent granted, **approved** (lineage `review-f093dd5f3f045c92`, acknowledged, authority burned), three non-blocking advisory findings. The stop hook then re-offered the selectorless candidate from `7a05ed94` — same 9 paths but 2960 lines, re-dragging T2.1's already-burned work — and it was **declined** (`review-e65b1481ae65786d`); candidate-scoped, reviews stay enabled. That re-offer is now **systematic, two for two** (T1.5 did the same from `ce090d3`). **All three advisory findings were verified against the code before acting, and only one survived**: R3-001 (the tests re-derived `distanceToGoalMouth` locally) **partially holds** — the duplication was real, but its own stated reason was false, since the function is `internal`, not `private`, and the same file already called it directly at lines 301 and 310; the helper is deleted and its three call sites now use `geometry.distanceToGoalMouth(for:)`. R3-002 (`nearFarExitCrossings` may emit a duplicate at a tangency) **does not hold**: it needs a bit-exact `0.0` from `atan2`/`sin`/`cos`/`sqrt` at one of ~180 grid samples, and even then `radialArcPoints` feeds its angles through a `Set`, so there is no observable defect to fix. R3-003 (`everyPolygonHasAtLeastThreeVertices` only runs on `.standard`) **does not hold**: `angleBoundsDegrees(for:)` returns five fixed non-empty ranges independent of every geometry value, so `count >= 3` holds by construction for any constructible `CourtGeometry`, and non-standard coverage of `shape(for:)` already exists in `shapeRoundTripsOnNonDefaultGeometry`.

## Day 3 — Tue Sep 22 (entry)

- [x] T3.1 Rival team + roster editor, "+" tile for unknown numbers — **decided before implementation**: deleting a player who already has recorded shots is **blocked**, not allowed and not soft-deleted. The schema's `.nullify` inverse would let the delete through and leave `shot.shooter` nil, so the shots would survive with no owner and that shooter's card would vanish from the statistics — a quiet wrong answer in the data the whole app exists to read. The rule is enforced in `Domain/` where it is testable, and surfaced in the editor as an explicit reason rather than a disabled control with no explanation. Scope also covers wiring `DemoDataSeeder.seed(into:)`, which has existed since T1.5 and is still called from nowhere; this is the first screen that would show it. Shirt numbers run **1...99** (IHF), confirmed by the user. Split into two work units for the same reason as T1.2, T2.1 and T2.2 — the app target has no test target. **Unit A (done)**: `c9d1aba` the T2.3 record and its one surviving advisory finding, `1b1f33f` `Domain/Sources/KeepercentDomain/Roster.swift` with `Roster`, `RosterError`, `RivalTeam`, `RivalTeamError` — delegated writer, strict TDD, RED shown as a compile failure against nonexistent types before GREEN. Storage is `[Int: Player]` keyed by shirt number, so a duplicate is **unrepresentable** rather than checked: a dictionary cannot hold two players at one key. `remove(number:recordedShotCount:)` cannot be called without stating the shot count, so the blocked-removal rule is an invariant of the signature instead of something a caller must remember. Parent corrections over the writer: a negative `recordedShotCount` now **traps**, because it cannot come from counting anything and letting it through rendered "has -1 recorded shots" to the user — the same loud-failure reasoning as the `GoalGeometry.init` and `CourtGeometry.init` traps; and `init(players:) throws` was added so the container never folds `StoredPlayer` rows into a roster inline in a SwiftUI view, where the absent test target would make that logic permanently untestable (it throws on the first duplicate rather than dropping a row, since a roster one player short misattributes every statistic derived from whoever vanished). Verified: `swift test --package-path Domain`: 205 tests / 62 suites passed (parent spot check re-ran it). RDD: **two consent envelopes were open at once on the same tree**, and both were relayed to the user, who granted one and declined the other. Granted: `--base-ref 718d068 --committed-only`, 4 paths, 505 lines, lens `review-reliability` -> **approved** (lineage `review-8517cf505fda0925`, acknowledged, authority burned), no blockers, three `SUGGESTION` findings. Declined: the stop hook's selectorless candidate from `7a05ed94`, 11 paths and **3431 lines**, re-dragging T2.1, T2.2 and T2.3 — its own risk evidence called `CLAUDE.md` "an executable change", which is what re-dragging an old docs commit looks like (`review-f228029b470902ff`, `declined_this_candidate`). That re-offer is now **four for four**. All three advisory findings were verified before acting and **one was rejected**: R3-001 claimed that using `geometry.distanceToGoalMouth(for:)` in `nearFarBoundaryVerticesSitExactlyOnTheNineMeterLine` makes the test circular, but `radiusAtGoalMouthDistance` **does not call it** — it solves the quadratic in closed form (`linearCoefficient + √discriminant`) while the test measures with clamp-and-`hypot`, so the two are independent computations that agree only if both are right, which is a cross-check rather than a tautology. R3-002 and R3-003 both held: neither pinned the **order of the guards**, and that order is what the editor shows the user, so `remove` on an absent player with a nonzero shot count and `update` on an absent player with an out-of-range new number are now tested to report the missing player rather than the second broken rule. 207 tests / 62 suites. **Unit B (done)**: `24d7e2c` those two guard-order tests, as the first commit of the unit per the freeze rule; `f1530a1` the editor itself, in `Keepercent/Features/Teams/` (`TeamsView`, `RosterEditorView`, `RosterGridView`, `PlayerTileView`, `PlayerEditorView`, `AddUnknownPlayerView`, `NewTeamSheet`, `RosterErrorMessaging`), with `KeepercentApp` now launching into `TeamsView` and `ContentView` kept as the T2.x test bench behind a toolbar button. **The write-back touches exactly one row**: it never diffs two `Roster` values and never replaces `team.players` wholesale. Each action rebuilds a fresh `Roster` from `team.players.map(\.domainPlayer)` and runs the domain operation only to validate against the whole current roster — if it throws, SwiftData is not touched — and then applies the single row change that action already knows: `addUnknown` inserts `StoredPlayer(Player(number:))`, built from the domain's own defaults so the row and the validated value cannot disagree; `update` mutates that instance's four fields in place; `remove` deletes that row. No other row is touched, so no other player's `shotsTaken`/`shotsFaced` is disturbed — deleting and re-inserting the roster would have churned relationships, the exact orphaned-shots outcome this task exists to prevent. `remove` passes the real `stored.shotsTaken.count`, never a hard-coded 0. "Remove Player" is always enabled: disabling it would make the view compute "has shots", which is the rule `Roster.remove` owns. Checked on the simulator with #4, who has 8 recorded shots: the editor shows "#4 has 8 recorded shots and can't be removed.", rendered from the error payload, and #4 stays on the grid; the edit sheet also shows "Recorded shots: 8" before the attempt. Goalkeepers (#1, #12) are marked by a heavy accent border **and** a glove badge — two channels, not colour alone. `DemoDataSeeder.seed(into:)` is finally wired, from `TeamsView`'s `.task`, the first place with a real `ModelContext`; a seeding error surfaces as an alert instead of being swallowed. **The defect green tests could not see, found by running it (the fifth view task in a row)**: the rejection `.alert` was declared on the parent view, and with a sheet presented SwiftUI dismisses the sheet to show the alert — so a refused removal **threw away unsaved name, goalkeeper and handedness edits**. A refusal must not cost unrelated work; the alert moved inside the `.sheet` closure, and a re-check by tapping shows the sheet staying open behind the message with its edits intact. Verified: `swift test --package-path Domain`: 207 tests / 62 suites passed; `xcodebuild -project Keepercent.xcodeproj -scheme Keepercent -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.1' build`: `** BUILD SUCCEEDED **`; run by hand on the simulator. RDD: closing slice with `--base-ref 1b1f33f --committed-only`, 12 paths, 881 lines, lens `review-reliability` -> **approved** (lineage `review-b84cd8a833811365`, acknowledged, authority burned), so no blocking findings. **The text of that review's advisory findings was lost**: the `capture-result` output was filtered before being saved, and the single-use context cannot be replayed, so whether it raised any advisory findings is unknown — not "none". The capture output is now dumped raw to a file before any filtering. The stop hook's selectorless candidate from `7a05ed94` — 20 paths, 4310 lines, re-dragging T2.1 through T3.1 unit A — was **declined** (`review-a974fb1f2120e057`, `declined_this_candidate`); reviews stay enabled.
- [x] T3.2 Session setup (live | video) — **decided before implementation**: the session's date is the **match date**, not the entry date, chosen with a date picker that starts on today (confirmed by the user). For a live session they coincide, but video scouting happens days after the match was played. A match date later than today is rejected in `Domain/`, compared by calendar day with an injected calendar and "today", and the picker cannot reach it either. The **active rival goalkeeper is not a session field**: she can change mid-match, so she belongs to shot entry (T3.3) and is already recorded per shot as `Shot.facingGoalkeeper`, which keeps `docs/mvp.md` §5's `Session (date, kind, opponent)` as it is. The rival is implicit: a session is started from that team's screen, which already shows its roster. There is no video URL or timestamp, since §9 says the app plays no video. Split into two work units for the same reason as T3.1 (the app target has no test target): **A** a `Session` value in `Domain/` holding the match-date rule, strict TDD; **B** a new-session sheet and the team's session list in `Keepercent/Features/Sessions/`, where a session opens a placeholder screen that T3.3 fills in. Route: A delegated writer (2 files, TDD); B delegated writer (2+ non-trivial view files), verified by running it on the simulator. `34a144c` the T3.1 record (passive, first commit of this task per the freeze rule). **Unit A (done)**: `06ec2cf` `Session` and `SessionError.matchDateInFuture` in `Domain/Sources/KeepercentDomain/Session.swift`, with `SessionTests.swift` — delegated writer, strict TDD, RED shown as a compile failure against the missing types. The comparison runs on `calendar.startOfDay(for:)` for both dates, never on the raw instants, and `matchDate` is stored as given. The time-zone test holds a fixed pair of instants — 23:30 UTC on Sep 20 against 00:30 UTC on Sep 21 — and checks that it is rejected through a UTC calendar but accepted through Europe/Paris, where both have already rolled to Sep 21. That is the proof that the injected calendar decides the day. 213 tests / 63 suites. **Unit B (done)**: `4f65205` `Keepercent/Features/Sessions/` (`NewSessionSheet`, `SessionMessaging`, `SessionRowView`, `SessionView`), plus the team's session list and "New Session" action in `RosterEditorView` — delegated writer. `startSession` follows T3.1's shape: build the domain `Session` first, and only if it succeeds insert exactly one `StoredSession` for this team, save, and push it. Starting a session therefore opens it the same way tapping an existing one does. The sheet's error alert is declared inside the sheet, the lesson from T3.1's lost edits. `RosterGridView` gained `isScrollable` so the grid and the session list share one outer `ScrollView` instead of nesting two. A stored session whose `kindCode` does not decode renders as "Unknown kind" rather than crashing or vanishing. Parent corrections over the writer: the `.sheet` chain had been left mis-indented inside the new `NavigationStack` and was straightened; and **`TeamsView` now gives `RosterEditorView` one identity per team** (`.id(selectedTeam.persistentModelID)`). Without it SwiftUI reuses the editor when the sidebar selection changes, so on iPad, where both columns are visible, the previous team's open session would have survived in its `NavigationPath`. Verified: `swift test --package-path Domain`: 213 tests / 63 suites; `xcodebuild ... -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.1' build`: `** BUILD SUCCEEDED **`. Run by hand on the **iPhone 17 Pro**: the date picker greys out every day after today; a video session on Sep 19 was created and opened directly; after a relaunch it was still there, listed once, newest match first. Run by hand on the **iPad Pro 11-inch (M5)**, the first time this project has been checked on an iPad: a session opens in the detail column with its back button; with BM Serra's session open, selecting a second team (created by the user) showed that team's empty roster, with no stale session and no "Session Not Found". The iPad simulator's input tooling was unreliable (taps landed late and typing failed), which is why the user created that second team. RDD: assessed with `--base-ref 34a144c --committed-only` — medium (`executable_change` in `Session.swift`), 10 paths, 587 lines, `review_due` (`slice_budget_reached`). The stop hook re-offered the selectorless candidate from `7a05ed94` twice this task: after `34a144c` (20 paths, 4310 lines, `review-a327013ec34753c6`) and after `4f65205` (26 paths, 4835 lines, `review-f5640de574bda7ad`). Both were **declined** (`declined_this_candidate`). The re-offer is now six for six. The real slice was **granted**: lens `review-reliability` -> **approved** (lineage `review-9264ad009be2018c`, acknowledged, authority burned), no blockers, two advisory findings. **This time the capture output was saved raw before any filtering, so the text of both findings is on record.** Both were verified against the code before acting, and both held. R3-001 (WARNING): in `startSession` the new row is appended and inserted before `try modelContext.save()`, and the `catch` only set the message. A failed save therefore left the new session on screen next to the alert saying it was not stored. The finding named one call site, but the same shape was in **five**: `addUnknown`, `performUpdate` and `remove` from T3.1, `createTeam` in `TeamsView`, and `startSession`. The user chose to fix all five, so each `catch` now calls `modelContext.rollback()`. Every action saves immediately, so there are never other pending changes for it to discard. A save failure is reachable on this machine — the disk hit 100% on Sep 21. The finding's second claim, that the row would vanish after a restart, was **not verifiable**, because the context's autosave may still flush it. R3-002 (SUGGESTION): the test helpers force-unwrapped `TimeZone(identifier:)` and `calendar.date(from:)`. They now go through `#require`, and the suite's `utc` calendar is built in a throwing `init()`. Checked by breaking it on purpose: an invalid zone identifier failed exactly one test, with a readable issue, and the other five still ran. The fixes ride with this record as the first commit of T3.3, per the freeze rule.
- [ ] T3.3 Shot entry end to end: attacking side, active rival goalkeeper, optional chips
- [ ] T3.4 Shot log with undo/delete

## Day 4 — Wed Sep 23 (analysis)

- [ ] T4.1 `StatsEngine` + tests
- [ ] T4.2 Linked-view component (court ↔ goal) with Swift Charts
- [ ] T4.3 Shooter card
- [ ] T4.4 Rival goalkeeper card

## Day 5 — Thu Sep 24 (AI + sharing) · feature freeze tonight

- [ ] T5.1 `InsightWriter` protocol + template fallback + tests
- [ ] T5.2 `FoundationModelsInsightWriter` with availability check
- [ ] T5.3 Shareable report card (`ImageRenderer` + `ShareLink`)
- [ ] T5.4 Roster import: Vision OCR + PDFKit + review screen *(first to cut)*

## Day 6 — Fri Sep 25 (ship)

- [ ] T6.1 Bug fixing, iPad/iPhone polish, accessibility pass
- [ ] T6.2 README: screenshots/GIF, architecture, AI disclosure
- [ ] T6.3 Final commit, tag, submit the repository link

## Notes

- Dates in the day headings follow `docs/mvp.md` §10; the plan there is authoritative.
- Route per task (inline vs delegated) is recorded as work progresses.
